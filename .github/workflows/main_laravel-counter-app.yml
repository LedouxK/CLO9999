# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Laravel app
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

# Éviter les actions en double
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    uses: ./.github/workflows/action.yml
    secrets:
      AZURE_APP_NAME: ${{ secrets.AZURE_APP_NAME }}
      AZURE_PUBLISH_PROFILE: ${{ secrets.AZURE_PUBLISH_PROFILE }}
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      APP_KEY: ${{ secrets.APP_KEY }}
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_84D096A159A24631977C17CB9D8E9E91 }}

  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Debug Secrets
        run: |
          echo "Vérification de la présence des secrets:"
          echo "AZURE_APP_NAME est défini: ${{ secrets.AZURE_APP_NAME != '' }}"
          echo "AZURE_PUBLISH_PROFILE est défini: ${{ secrets.AZURE_PUBLISH_PROFILE != '' }}"
          echo "AZURE_CLIENT_ID est défini: ${{ secrets.AZURE_CLIENT_ID != '' }}"
          echo "AZURE_CLIENT_SECRET est défini: ${{ secrets.AZURE_CLIENT_SECRET != '' }}"
          echo "AZURE_TENANT_ID est défini: ${{ secrets.AZURE_TENANT_ID != '' }}"

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, mysql, zip
          coverage: none

      - name: Check if composer.json exists
        id: check_files
        uses: andstor/file-existence-action@v1
        with:
          files: 'composer.json'

      - name: Get composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache composer dependencies
        uses: actions/cache@v3
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Create .env file
        run: |
          cp .env.example .env
          echo "APP_KEY=${{ secrets.APP_KEY }}" >> .env
          echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
          echo "APP_ENV=production" >> .env
          echo "APP_DEBUG=false" >> .env
          echo "DB_CONNECTION=mysql" >> .env
          echo "DB_HOST=laravelmysqlsrv.mysql.database.azure.com" >> .env
          echo "DB_PORT=3306" >> .env
          echo "DB_DATABASE=laraveldb" >> .env
          echo "DB_USERNAME=mysqladmin" >> .env
          echo "WEBSITE_DOCUMENT_ROOT=/home/site/wwwroot/public" >> .env

      - name: Set Laravel Permissions
        run: |
          chmod -R 777 storage bootstrap/cache
          chown -R www-data:www-data .
          find . -type f -exec chmod 644 {} \;
          find . -type d -exec chmod 755 {} \;

      - name: Configure MySQL Firewall
        env:
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_84D096A159A24631977C17CB9D8E9E91 }}
        run: |
          # Récupérer l'IP publique de l'exécuteur GitHub Actions
          PUBLIC_IP=$(curl -s https://api.ipify.org)
          echo "IP publique de l'exécuteur: $PUBLIC_IP"
          
          # Connexion à Azure
          az login --service-principal \
            --username $AZURE_CLIENT_ID \
            --password $AZURE_CLIENT_SECRET \
            --tenant $AZURE_TENANT_ID

          # Définir la subscription
          az account set --subscription $AZURE_SUBSCRIPTION_ID
          
          # Ajouter une règle de pare-feu temporaire pour GitHub Actions
          az mysql flexible-server firewall-rule create \
            --resource-group laravel-rg \
            --name laravelmysqlsrv \
            --start-ip-address $PUBLIC_IP \
            --end-ip-address $PUBLIC_IP \
            --rule-name allow-github-actions

      - name: Run Migrations
        run: |
          php artisan migrate --force
          php artisan cache:table
          php artisan session:table

      - name: Remove MySQL Firewall Rule
        if: always()
        env:
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_84D096A159A24631977C17CB9D8E9E91 }}
        run: |
          # Reconnexion à Azure (au cas où la session a expiré)
          az login --service-principal \
            --username $AZURE_CLIENT_ID \
            --password $AZURE_CLIENT_SECRET \
            --tenant $AZURE_TENANT_ID

          # Définir la subscription
          az account set --subscription $AZURE_SUBSCRIPTION_ID

          # Suppression de la règle de pare-feu
          az mysql flexible-server firewall-rule delete \
            --resource-group laravel-rg \
            --name laravelmysqlsrv \
            --rule-name allow-github-actions \
            --yes

      - name: Clear Config
        run: |
          php artisan config:clear
          php artisan cache:clear
          php artisan view:clear
          php artisan route:clear
          php artisan event:clear

      - name: Optimize
        run: |
          php artisan optimize:clear
          php artisan optimize

      - name: Zip artifact for deployment
        run: zip release.zip ./* .env.example .deployment -r

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: php-app
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: php-app

      - name: Unzip artifact for deployment
        run: unzip release.zip
      
      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: ${{ secrets.AZURE_APP_NAME }}
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
          package: .

      - name: Verify Deployment
        run: |
          echo "🔍 Vérification du déploiement..."
          echo "📂 URL de l'application: https://${{ secrets.AZURE_APP_NAME }}.azurewebsites.net"
          echo "📂 URL Kudu (SCM): https://${{ secrets.AZURE_APP_NAME }}.scm.azurewebsites.net"
          
          # Attendre que l'application soit disponible
          sleep 30
          
          # Vérifier si l'application répond
          curl -I "https://${{ secrets.AZURE_APP_NAME }}.azurewebsites.net"

      - name: Force deployment if needed
        if: failure()
        env:
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_84D096A159A24631977C17CB9D8E9E91 }}
        run: |
          echo "⚠️ Tentative de déploiement forcé..."
          
          # Connexion à Azure
          az login --service-principal \
            --username $AZURE_CLIENT_ID \
            --password $AZURE_CLIENT_SECRET \
            --tenant $AZURE_TENANT_ID

          # Définir la subscription
          az account set --subscription $AZURE_SUBSCRIPTION_ID
          
          # Forcer le déploiement
          az webapp deployment source config-zip \
            --resource-group laravel-rg \
            --name ${{ secrets.AZURE_APP_NAME }} \
            --src release.zip
          